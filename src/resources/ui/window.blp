using Gtk 4.0;

template $TwinkAppWindow : ApplicationWindow {
  default-width: 600;
  default-height: 300;
  title: _("Hello, Blueprint!");

  [titlebar]
  HeaderBar {}

  Label {
    label: _("This is a label.");//bind TwinkAppWindow.main_text;
  }
}